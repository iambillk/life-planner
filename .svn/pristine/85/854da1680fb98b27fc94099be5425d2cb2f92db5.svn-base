{# ===================================================================
   templates/realestate/quick_add.html
   Quick Add Maintenance - Placeholder Template
   Version: 0.1.0 (Stub)
   Created: 2025-01-03
   
   PLACEHOLDER: This template is a stub for future implementation
=================================================================== #}

{% extends "base.html" %}
{% block title %}Quick Add - {{ property.name }}{% endblock %}
{% block header %}Quick Add Maintenance{% endblock %}

{% block extra_css %}
<style>
  .placeholder-container {
    max-width: 900px;
    margin: 0 auto;
    padding: 2rem;
    text-align: center;
  }
  
  .placeholder-box {
    background: var(--panel, #121a2a);
    border-radius: 12px;
    padding: 3rem;
    box-shadow: 0 4px 20px rgba(0,0,0,0.3);
  }
  
  .placeholder-icon {
    font-size: 4rem;
    margin-bottom: 1rem;
    opacity: 0.5;
  }
  
  .placeholder-title {
    color: var(--text, #e6eaf2);
    font-size: 1.5rem;
    margin-bottom: 1rem;
  }
  
  .placeholder-message {
    color: var(--text-muted, #9fb0c8);
    margin-bottom: 2rem;
  }
  
  .quick-buttons {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 1rem;
    margin: 2rem auto;
    max-width: 600px;
  }
  
  .quick-btn {
    background: var(--card, #0f1625);
    padding: 1.5rem;
    border-radius: 8px;
    border: 2px solid var(--line, #1f2a3d);
    opacity: 0.5;
    cursor: not-allowed;
  }
  
  .quick-btn-icon {
    font-size: 2rem;
    margin-bottom: 0.5rem;
  }
  
  .quick-btn-label {
    color: var(--text, #e6eaf2);
    font-size: 0.9rem;
  }
  
  .btn-back {
    background: var(--primary, #6ea8ff);
    color: white;
    padding: 0.75rem 2rem;
    border-radius: 8px;
    text-decoration: none;
    display: inline-block;
    transition: all 0.2s;
  }
  
  .btn-back:hover {
    background: var(--primary-700, #3f7fe6);
    transform: translateY(-1px);
  }
  
  .coming-soon {
    background: var(--warning, #f59e0b);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    display: inline-block;
    margin-top: 1rem;
    font-size: 0.9rem;
  }
</style>
{% endblock %}

{% block content %}
<div class="placeholder-container">
  <div class="placeholder-box">
    <div class="placeholder-icon">⚡</div>
    <h2 class="placeholder-title">Quick Add Maintenance</h2>
    <p class="placeholder-message">
      Mobile-optimized quick entry coming soon! One-tap logging for common tasks at {{ property.name }}.
    </p>
    
    <div class="quick-buttons">
      <div class="quick-btn">
        <div class="quick-btn-icon">🌱</div>
        <div class="quick-btn-label">Mow Lawn</div>
      </div>
      <div class="quick-btn">
        <div class="quick-btn-icon">❄️</div>
        <div class="quick-btn-label">AC Filter</div>
      </div>
      <div class="quick-btn">
        <div class="quick-btn-icon">🚿</div>
        <div class="quick-btn-label">Add Salt</div>
      </div>
      <div class="quick-btn">
        <div class="quick-btn-icon">⚡</div>
        <div class="quick-btn-label">Generator Test</div>
      </div>
    </div>
    
    <div class="coming-soon">
      For now, use the regular Add Maintenance form
    </div>
    
    <br><br>
    
    <a href="{{ url_for('realestate.add_maintenance', property_id=property.id) }}" class="btn-back">
      Go to Add Maintenance →
    </a>
    
    <br><br>
    
    <a href="{{ url_for('realestate.property_detail', id=property.id) }}" class="btn-back">
      ← Back to Property
    </a>
  </div>
</div>
{% endblock %}